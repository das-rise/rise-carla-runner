`roundabout-saro-osm-to-xodr.xodr`:
Created with https://github.com/RISE-Dependable-Transport-Systems/osm-to-xodr/, default settings:
```
uvx osm-to-xodr convert roundabout-saro.osm
```

-----

`saro_fixed_bidirectional.xodr`:
Created using `Netedit` and `Netconvert` (from `Sumo`), streets pruned by hand.

-----